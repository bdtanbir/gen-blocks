/**
 * Simple Card Block - Save Component
 *
 * Returns null because this block uses server-side rendering (PHP).
 * The render() method in Block_Simple_Card handles the frontend output.
 */

export default function save() {
    return null;
}
